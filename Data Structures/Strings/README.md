# Strings in Python

## Overview

Strings are one of the most commonly used data types in Python. A string is a sequence of characters enclosed in single quotes (`' '`), double quotes (`" "`), or triple quotes (`''' '''` or `""" """`).

This module covers the fundamentals of working with strings, including indexing, slicing, formatting, built-in methods, and real-world applications.

---

## Topics Covered

### 1. String Indexing & Slicing

Learn how to access individual characters and extract parts of a string.

**Concepts:**

* Positive Indexing
* Negative Indexing
* Slicing
* Step Value

---

### 2. String Methods

Practice commonly used built-in string methods such as:

* `upper()`
* `lower()`
* `title()`
* `capitalize()`
* `strip()`
* `replace()`
* `find()`
* `count()`
* `split()`
* `join()`
* `startswith()`
* `endswith()`
* `isalpha()`
* `isdigit()`
* `isalnum()`

---

### 3. f-Strings

Learn modern string formatting using f-strings.

**Examples Covered:**

* Variable interpolation
* Formatting expressions
* Cleaner and more readable output

---

### 4. Real-World String Examples

Practice solving practical problems such as:

* Email validation
* Password masking
* Phone number formatting
* Username generation
* Extracting information from text
* URL checking

---

### 5. Practice Programs

Additional coding exercises to strengthen string manipulation skills and logical thinking.

---

## Skills Practiced

* Working with string data
* String indexing and slicing
* Using built-in string methods
* String formatting with f-strings
* Solving real-world text processing problems
* Writing clean and efficient Python code

---

## Learning Outcomes

After completing this module, I learned:

* How strings are stored and manipulated in Python.
* Different ways to access and modify string data.
* How to use built-in string methods effectively.
* Modern string formatting using f-strings.
* Applying string manipulation to real-world programming scenarios.

---

**Author:** Paras

**Module:** Strings

